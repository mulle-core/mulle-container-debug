### 0.1.2




* container-debug now exports its public symbols when compiled as part of mulle-core `(MULLE__CORE_BUILD),` preventing missing-symbol/linkage issues for embedded builds
* include asset/dox/TOC.md in package sources so documentation is distributed with the library
